import { ScheduleRuleIdPipe } from './schedule-rule-id.pipe';

describe('ScheduleRuleIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleRuleIdPipe();
    expect(pipe).toBeTruthy();
  });
});
